# 차량 인스턴스 세그멘테이션 학습·평가 (Colab: torch/torchvision 은 이미 설치돼 있음)
numpy
opencv-python-headless
pyyaml
tqdm
pycocotools>=2.0.7
pillow
torch>=2.1
torchvision>=0.16

# 모델 계열별 — 쓰는 것만 설치해도 된다
ultralytics>=8.4.0        # YOLOv8 / YOLO11 / YOLO26 -seg
transformers>=4.40        # Mask2Former
timm                      #   (Swin 백본 일부 버전에서 필요)
rfdetr[train,loggers]>=1.3  # RF-DETR Segmentation (학습에는 [train] 추가 의존성 필요)
