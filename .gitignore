# 원본 데이터 (AI-Hub 데이터는 재배포 금지, 용량도 GitHub 한도 초과 → Google Drive 로 공유)
/images/
/*.zip

# 개발 환경
.venv/
__pycache__/
.DS_Store

# 모델 가중치 (다운로드하거나 다시 학습)
road_lane/weights/
road_lane/outputs/**/*.pt

# 실험 결과 폴더 (ROAD_LANE_OUT=runs/이름)
road_lane/runs/

# road_lane 실행 결과 전체 (run_all.sh 로 다시 만듦)
# 기준 결과가 필요하면 정리 직전 커밋에서: git checkout 206e23c -- road_lane/outputs
road_lane/outputs/

# 설명용 그림 (docs/build_figures.py 로 다시 만듦)
road_lane/docs/fig/
