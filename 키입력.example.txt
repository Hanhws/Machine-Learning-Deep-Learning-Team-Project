# ───────────────────────────────────────────────
#  이 파일을 복사해서 "키입력.txt" 로 저장한 뒤 값을 채우세요.
#  키입력.txt 는 .gitignore 에 걸려 있어 커밋되지 않습니다.
#
#  = 뒤에 값만 붙여넣으세요. 따옴표 없이, 한 줄에 하나씩, 줄바꿈 없이.
#  ITS_API_KEY 만 필수입니다.
# ───────────────────────────────────────────────

# 국가교통정보센터 CCTV Open API 인증키 (필수)
#   https://www.its.go.kr/opendata/opendataList?service=cctv
#   32자 안팎의 한 줄짜리 문자열입니다.
ITS_API_KEY=

# 허깅페이스 read 토큰 — SAM3 가중치(3.3GB) 다운로드용 (선택)
#   1) https://huggingface.co/facebook/sam3 에서 "Agree and access repository" 먼저 클릭
#   2) https://huggingface.co/settings/tokens 에서 Read 토큰 발급
#   hf_ 로 시작합니다. 비워두면 SAM2.1 로 자동 대체됩니다(텍스트 프롬프트만 불가).
#   가중치를 한 번 받고 나면 더 이상 필요 없으니 폐기하세요.
HF_TOKEN=

# ngrok — 집 인터넷은 공유기 뒤라 밖에서 직접 들어올 수 없어 터널이 필요합니다 (선택)
#   토큰:   https://dashboard.ngrok.com  →  Your Authtoken
#   도메인: 같은 대시보드  →  Network  →  Domains  에서 무료 고정 도메인 1개 발급
#   도메인은 https:// 를 빼고 호스트명만 넣으세요. 예: brave-fox-123.ngrok-free.dev
#   둘 중 하나라도 비면 터널을 건너뛰고 이 PC 에서만 접속됩니다.
NGROK_AUTHTOKEN=
NGROK_DOMAIN=
